module ULA (S, Sel, A, B, Cin);

	input [3:0] A, B;
	input [2:0] Sel;
	output [7:0] S; 
	input Cin;//add mais uma saida
	
	wire [7:0] Soma, Sub, Multi, Div, AndOp, OrOp, XorOp;
	
	// OPERAÇOES
	
	somador4x4(S, Cin, A, B, 1'b0);
	subtrator4x4(Ss, Cin, A, B, 1'b0);
	multiplicador4x4(Multi, A, B);
	
	// MULTIPLEXADOR
	
	multiplexador8x4(S, decosel, Soma, Sub, Multi, Div, AndOp, OrOp, XorOp); 
	
	// DECODIFICADOR DISPLAY
	
endmodule
	
	