module muxdivisorbase (

    output Z,
    input A, // Q (seletor)
    input B, // resto
    input S // borrow
);
    wire nA;
    wire t1, t2;
	 
    not (nA, A); // ~A
    and (t1, nA, S); // ~A & S
    and (t2, A, B); // A & B
    or (Z, t1, t2); // Z = (~A & S) | (A & B)
	 
endmodule